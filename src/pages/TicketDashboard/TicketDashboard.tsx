import { TicketDashboardContainer } from '@containers/TicketDetails/TicketDetails';

import { PageWrapper } from './TicketDashboard.styles';

export const TicketDashboardPage = () => (
    <PageWrapper>
        <TicketDashboardContainer />
    </PageWrapper>
);

import { Stack, styled } from '@mui/material';

export const HeaderStack = styled(Stack)(() => ({
    flexDirection: 'row',
    justifyContent: 'end',
    alignItems: 'center',
    width: '100%',
}));
